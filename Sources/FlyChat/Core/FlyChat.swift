/// Root namespace for FlyChat SDK constants and metadata.
///
/// Use this enum to access SDK-level information such as the current version string.
/// This type is not intended to be instantiated.
public enum FlyChat {
    /// Current SDK semantic version string.
    public static let version = "0.2.0"
}
